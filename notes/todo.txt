Dockerfile+image
 - need to better organise the image directories
   - sw for downloaded software
   - commit image and save to dockerhub

Pipeline (volunteer)
- allow user to get accessions of interest SRR,SRX,SRA,GSE,etc

Frontend
- allow more elegant dataset sanitation and transfer to backend
  - if "started" file is older than x minutes then delete
  - dont allow new uploads to fill the disk
  - if backend server is not available, then don't transfer or delete zip archive
